# Fixture: workflow with minimal correct permissions (for testing only — mock data)
name: Minimal Permissions

on: [push]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
