# Fixture: workflow with known-CVE actions (for testing only — mock data)
name: CVE Test

on: [push]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # GOOD: SHA-pinned, should NOT trigger CVE finding
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      # BAD: CVE-2025-30066 — tj-actions supply-chain attack
      - uses: tj-actions/changed-files@v35

      # BAD: CVE-2025-30154 — reviewdog supply-chain attack
      - uses: reviewdog/action-setup@v1
