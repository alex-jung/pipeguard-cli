# E2E fixture: fully secure workflow — all scanners should pass
name: E2E All Clean

on: [push]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Run tests
        run: pytest
